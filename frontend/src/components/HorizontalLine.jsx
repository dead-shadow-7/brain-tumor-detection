const HorizontalLine = ({ color }) => (
  <hr
    style={{
      color: color,
      backgroundColor: color,
      height: 1,
      width: "95%",
      marginTop: "8rem",
      borderRadius: "0.25rem",
      border: "none",
    }}
  />
);

export default HorizontalLine;
